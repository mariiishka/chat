import {type FC} from 'react';

const Header: FC = () => {
  return (
    <div className="bg-gray-200 text-slate-200 p-4 flex justify-center">
      Simple header
    </div>
  );
};

export default Header;
